[global]
# Left click should execute the notification's default action first.
# If the app supports it, this usually focuses the app and lets the
# compositor move you to the relevant workspace.
mouse_left_click = do_action, close_current

# Keep useful defaults for the other buttons.
mouse_middle_click = do_action, close_current
mouse_right_click = close_all
